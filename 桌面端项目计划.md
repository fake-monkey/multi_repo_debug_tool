# `repo_debug` 桌面端项目计划

## 当前阶段

当前只修正产品定位并确定项目布局，不创建 Tauri 脚手架，不改造现有 `multi_repo_debug_tool`，不实现新的 worktree 管理功能，也不改动已有编译功能。

## 项目定位

### 当前现状

当前的 `repo_debug` 是面向多仓库本地开发场景的编译工具，尚未把多 worktree 纳入自身模型。

它当前解决的是：一个产品由多个有依赖关系的仓库共同组成，配置为每个仓库提供一个实际目录，工具基于这些目录构建 Conan 依赖关系，并依次执行仓库准备、CMake configure、build/install、`compile_commands.json` 生成、解决方案合并和运行时文件收集。

如果同一个仓库存在多个 worktree，当前工具不会主动发现它们、不会记录它们之间的关系，也不能在一次编译前让用户选择要使用哪个 worktree。用户必须在工具外自行完成这些工作，例如手动准备目标目录，并确保本次使用的配置指向正确的目录。

### 现有痛点

多项任务并行开发时，同一个仓库可能存在多个 worktree。此时每次编译前都需要人工确认多个仓库分别应该使用哪个 worktree，并手动调整目录或配置。`repo_debug` 只能接收最终路径，不能帮助用户完成选择，也无法清楚展示当前这组路径究竟对应哪些分支和提交。

这容易出现以下问题：

- 某个依赖仓库仍指向上一次任务使用的 worktree。
- 多个仓库的分支组合不属于同一个开发任务，但直到配置或编译阶段才暴露问题。
- 用户需要记忆或反复检查每个 worktree 的路径、分支、HEAD 和 dirty 状态。
- 一套已经验证可用的多仓库目录组合无法作为明确的编译工作区保存和恢复。

### 未来目标

桌面端计划在现有多仓库编译能力之上补充多 worktree 管理：

- 发现每个仓库已有的 worktree，并读取其路径、分支、HEAD 和工作区状态。
- 在一次编译前，为每个参与仓库明确选择一个 worktree。
- 将选定的 worktree 组合保存为可识别、可恢复的编译工作区。
- 把选择结果解析成具体仓库路径，再交给现有 Conan/CMake 编译主链路。
- 在需要时提供 worktree 创建、切换和移除入口，并在写操作前执行安全检查。
- 通过前端展示编译工作区、编译选项、执行进度、错误和恢复操作。

因此，未来产品仍然是编译工具，不是通用 Git 仓库管理器。仓库和 worktree 管理能力只服务于“准备并执行一套可编译的多仓库工作区”。

## 命名决定

- 产品名和 CLI 名继续使用 `repo_debug`。
- 现有 Rust 项目目录继续使用 `multi_repo_debug_tool`。
- 现有 Rust package 继续使用 `repo_debug`。
- 新增桌面端目录使用 `desktop`。
- Tauri 壳的 Rust package 使用 `repo_debug_desktop`，它不是第二套编译工具，只是 `repo_debug` 的桌面入口。
- 不再创建顶层 `repo_manager` 项目。

这套命名保留了现有 CLI 和安装方式，也准确表达了桌面端与现有编译工具之间的从属关系。

## 与现有 `multi_repo_debug_tool` 的关系

桌面端必须复用现有编译主链路，不能在 Tauri 后端重新实现一套：

- `FileConfig` / `RepoConfig` 配置读取与运行时上下文构造。
- 仓库准备以及实际仓库路径更新。
- Conan 依赖图和拓扑顺序。
- 增量任务、指纹、完成状态和中断恢复。
- Conan install。
- CMake configure、build 和 install。
- `compile_commands.json` 生成。
- `.sln` 合并和 DLL 收集。
- Visual Studio / Ninja 环境处理和已有的定向构建修复。

当前这些能力主要由 `multi_repo_debug_tool/src/main.rs` 组装，并且该 package 还没有 `lib.rs`。在接入桌面端前，第一项 Rust 主改动应是把现有主流程暴露为库入口，同时保留 CLI：

```text
CLI 参数 -> repo_debug library -> 现有编译流程
Tauri IPC -> repo_debug library -> 同一套编译流程
```

桌面端不能通过启动 `repo_debug.exe` 并解析控制台文本来获得核心状态。这种方式可以运行命令，却无法稳定表达仓库拓扑、任务状态、结构化错误、进度和取消语义。

## 总体项目布局

```text
study_rust/
├── Cargo.toml
├── Cargo.lock
└── multi_repo_debug_tool/
    ├── Cargo.toml                    # package: repo_debug，编译核心 + CLI
    ├── README.md
    ├── 桌面端项目计划.md
    ├── src/
    │   ├── lib.rs                    # 后续新增：编译核心的公共组装入口
    │   ├── main.rs                   # 后续收窄：CLI 解析、日志初始化、退出码
    │   ├── cli.rs
    │   ├── config.rs
    │   ├── repo.rs
    │   ├── task.rs
    │   ├── conan_executor.rs
    │   ├── cmake_configure.rs
    │   ├── cmake_build_install.rs
    │   ├── compile_commands.rs
    │   └── ...                       # 其余现有编译模块保持原职责
    └── desktop/                      # 新增：repo_debug 桌面入口
        ├── package.json
        ├── pnpm-lock.yaml
        ├── index.html
        ├── vite.config.ts
        ├── tsconfig.json
        ├── src/                      # React / TypeScript
        └── src-tauri/                # Tauri Rust 壳
            ├── Cargo.toml            # package: repo_debug_desktop
            ├── build.rs
            ├── tauri.conf.json
            ├── capabilities/
            ├── icons/
            └── src/
```

根 Cargo workspace 后续同时管理：

- `multi_repo_debug_tool`：`repo_debug` library + CLI binary。
- `multi_repo_debug_tool/desktop/src-tauri`：依赖 `repo_debug` library 的桌面 binary。

整个仓库仍只使用根目录的 `Cargo.lock`。JavaScript 依赖只存在于 `multi_repo_debug_tool/desktop`，暂不把整个仓库改成 pnpm workspace。

## 编译核心布局

现有 Rust crate 不需要为了桌面端重写成多 crate 架构。先在同一个 package 中形成“库 + CLI”即可：

```text
multi_repo_debug_tool/src/
├── lib.rs                            # 公开少量应用级入口
├── main.rs                           # 薄 CLI 适配层
├── cli.rs                            # clap 输入，只供 CLI 使用
├── workflow.rs                       # 主编译流程；确有拆分需要时再新增
├── config.rs                         # 文件配置和运行时上下文
├── config/
│   └── task_containers.rs
├── repo.rs                           # 仓库准备和 Git 流程
├── repo_config.rs
├── task.rs                           # 现有增量任务契约
├── conan_executor.rs
├── cmake_configure.rs
├── cmake_build_install.rs
├── compile_commands.rs
├── sln_merge_kit.rs
├── dll_copy_util.rs
└── ...
```

这里的 `workflow.rs` 只是现有 `main.rs` 业务流程可能的落点，不要求为了目录完整而立即创建。是否拆出以及拆出哪些函数，要以实际提取 library 入口时的调用关系为准。

编译核心继续采用可读的顺序工作流。不会因为加入前端就把 Conan、CMake 和跨仓库依赖改造成通用 DAG；真正需要并行的阶段以后在具体工作流中单独处理。

## 编译核心需要提供的应用级接口

桌面端不应直接调用 `CMakeConfigureTask`、`BuildInstallTask` 等内部任务。library 应提供面向一次编译的少量具体接口，例如：

```text
inspect_build_workspace(...)
run_build(...)
continue_build(...)
update_build_config(...)
```

接口所使用的数据应围绕真实编译过程命名，不建立抽象的 command bus 或任务节点系统。预计需要的具体数据包括：

- `BuildWorkspaceRequest`：配置文件、所选 worktree 路径和本次运行参数。
- `BuildWorkspaceSnapshot`：参与仓库、所选 worktree、分支、HEAD、dirty 状态、依赖顺序和配置摘要。
- `BuildProgress`：当前仓库、当前阶段、控制台输出和完成情况。
- `BuildResult`：成功、失败或取消，以及对应的结构化错误。

这些名称只是当前布局需要的接口形状，最终字段要在下一阶段的数据模型设计中确认。

## 多仓库与多 worktree 的数据关系

以下是桌面端未来需要补充的数据关系，不是当前 `FileConfig` 已经具备的模型。需要明确区分三类对象：

- 仓库定义：仓库标识、URL、Conan/CMake 配置及它在依赖关系中的角色。
- worktree：某个仓库的一个实际工作目录，包含路径、分支、HEAD 和工作区状态。
- 编译工作区：为一次本地联调选定的 worktree 组合，以及构建类型、生成器和本次执行选项。

```text
编译工作区
├── 主程序仓库 -> 选中的 worktree
├── 依赖仓库 A -> 选中的 worktree
├── 依赖仓库 B -> 选中的 worktree
└── 编译配置
```

同一个 Git 仓库可以有多个 worktree，但同一个编译工作区对一个仓库只能选择一个实际 worktree。编译核心最终消费的是已经解析完成的具体路径，不能依赖“当前打开目录”猜测用户想编译哪一个 worktree。

现有 `multi_repo_debug_param.json` 继续表达仓库、Conan 和 CMake 的编译语义。桌面端新增的“worktree 组合”是否写入该文件，或作为独立的编译工作区配置保存，将在数据模型阶段决定；当前不能直接把瞬时 UI 选择塞进 `FileConfig`。

## 前端布局

```text
multi_repo_debug_tool/desktop/src/
├── main.tsx                          # 前端入口，只挂载 App
├── app/
│   ├── App.tsx                       # 应用外壳和顶层布局
│   └── providers.tsx                 # 确有全局 provider 后再添加
├── features/
│   ├── build_workspaces/             # 编译工作区列表、创建和选择
│   ├── worktree_selection/           # 各仓库 worktree 查看与组合选择
│   ├── build_configuration/          # 构建类型和本次执行参数
│   └── build_runs/                   # 编译进度、日志、取消、失败和恢复
├── shared/
│   ├── api/                          # 对 Tauri command/channel 的类型化封装
│   ├── components/                   # 跨功能复用的纯 UI 组件
│   └── types/
└── styles/
```

前端功能围绕“准备并运行编译”组织，不设置通用的 `repositories` 管理首页。仓库和 worktree 信息首先出现在编译工作区的选择矩阵中；以后确有脱离编译流程的仓库维护需求，再增加独立页面。

第一阶段不预选全局状态库。编译工作区编辑状态先由对应 feature 持有；只有多个无父子关系的页面确实共享可变状态时，再决定是否引入。

## Tauri 壳布局

```text
multi_repo_debug_tool/desktop/src-tauri/src/
├── main.rs                           # 桌面入口，只调用 lib::run
├── lib.rs                            # 组装 Tauri、状态、命令和 Job Object
├── ipc/
│   ├── mod.rs
│   ├── build_workspaces.rs           # 工作区读取和保存
│   ├── worktrees.rs                  # worktree 查询和准备
│   └── build_runs.rs                 # 启动、继续、取消和进度 channel
├── state.rs                          # 运行中的编译任务和桌面状态
└── error.rs                          # 编译核心错误到 IPC 错误的边界转换
```

Tauri 壳保持薄层：

- 它依赖 `repo_debug` library，不复制编译流程。
- 短操作通过 Tauri command 返回结构化结果。
- 编译等长操作由 command 启动，通过 channel 按顺序发送阶段、仓库名、原生输出和结果。
- Tauri 的异步运行时只负责不阻塞 UI、转发进度和处理取消；现有阻塞式外部命令如何接入，需在执行模型阶段单独设计。
- 桌面进程必须继续接入现有 `job-guard`，确保取消、崩溃或窗口退出时不会遗留 CMake、MSBuild、`cl.exe` 等子进程。
- Rust 内部保留完整错误链和调用点上下文，到 IPC 边界时再转换成前端可以分层显示的结构。

## 依赖方向

```text
repo_debug CLI ───────┐
                      ├──> repo_debug library ──> Conan / CMake / Git / 文件系统
repo_debug_desktop ───┘
        ↑
React / TypeScript
```

- CLI 和桌面端是两个入口，不互相调用。
- 编译核心不能依赖 Tauri、React 或桌面状态。
- Tauri IPC 不直接操作内部任务容器，而是调用 library 的应用级编译接口。
- 前端不拼接 Git、Conan 或 CMake 命令。
- `git_tool` 仍可作为 Rust library 提供已有的 Git helper；是否继续依赖某个具体 helper，应按现有调用关系判断，不为桌面端再包一层同名能力。

## 第一阶段布局验收标准

进入脚手架和核心提取阶段后，布局需要满足：

- 现有 `repo_debug` CLI 的命令行入口和行为保持可用。
- `multi_repo_debug_tool` 同时生成可复用 library 和现有 CLI binary。
- `repo_debug_desktop` 作为第二个 workspace member 依赖该 library。
- 一个只读接口能返回当前配置下的仓库、实际路径、分支和编译顺序。
- 一个模拟长任务能通过 channel 顺序到达 `build_runs`，证明进度通道可用。
- CLI 与桌面端没有两份 Conan/CMake 编排代码。
- 不迁移现有 build 目录，不改变稳定的 `compile_commands.json` 输出位置。

## 后续计划待补内容

项目定位和布局确认后，再按顺序补充：

1. 第一版编译工作区的用户流程和页面信息架构。
2. 仓库定义、worktree 选择和编译工作区的数据模型。
3. 现有 `main.rs` 提取 library 入口的边界。
4. 编译进度、控制台输出、取消和中断恢复协议。
5. worktree 创建、切换和移除的安全规则。
6. 分阶段实现与真实临时仓库测试计划。
